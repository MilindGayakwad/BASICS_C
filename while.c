#include <stdio.h>

int main() {
    int count = 1;

    // While loop to print numbers from 1 to 5
    while (count <= 5) {
        printf("Count is: %d\n", count);
        count++; // Increment the counter
    }

    return 0;
}
